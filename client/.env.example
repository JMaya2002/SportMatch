# API: a qué backend habla el cliente
#   - "real" → fetch real contra /api (necesitas npm run dev:server)
#   - "mock" → datos de demostración en memoria (para GH Pages)
VITE_API_MODE=real

# URL base del backend (en dev Vite hace proxy de /api -> :3000)
VITE_API_URL=/api
